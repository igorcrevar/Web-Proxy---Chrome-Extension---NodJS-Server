Web Proxy chrome extension + nodejs server

Get desired page(photo, any other file) from desired banned domain via own web proxy server

start node server somewhere
add chrome extension

Set pages which will be redirected automaticly to web proxy
Open all other urls in web proxy mode if you wish

todo:
cookies

for now, server does not do http(s) request with cookies.
Works with relative urls, but if you open relative url in new tab you must be sure to use only one site at time.